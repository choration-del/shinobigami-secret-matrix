# v8候補版 デバッグ結果（2026-09-24）

## 対象と実行方法

対象は `シノビガミ秘密管理_v8_test.html`。localhostのWebサーバーをPlaywrightから起動し、Chromiumでクリック・入力・Canvas操作・ドラッグ・ダウンロード・ファイル選択を実行しました。console error / page errorは自動収集し、発生すればテストが失敗する構成です。

- 全体回帰: `npm run test:all`（環境確認2件、既存v8 23件、新規v8 25件）
- v8のみ: `npm run test:v8`
- 新規テストを画面表示して実行: `npm run test:v8 -- tests/shinobigami-v8-regression.spec.js --headed --trace on`
- 特定操作のデバッグ: `npm run test:v8 -- --debug -g "横スクロール"`
- レポート: `npm run test:report`

## 発見と修正

| 問題 | 原因と修正 |
| --- | --- |
| PC名をUndoしても入力欄が戻らない | 確定済み入力も下書き扱いで退避していた。確定名と異なる入力だけ保持する。 |
| リセット後も入力途中のPC名が残る | 下書きが初期化されていなかった。リセットで明示的に破棄する。 |
| JSON読み込み後に古いPC名や選択欄が残る | 古い下書き・DOM選択値が読み込んだ状態を上書きしていた。読み込み成功時は読み込んだ状態から表示する。 |
| 秘密名変更後に先頭の別秘密を選択してしまう | 更新した選択値を旧DOMの値で上書きしていた。更新済み選択を使う。 |
| 秘密名変更で別秘密の内部キーが衝突しデータが壊れる | 表示名だけを重複チェックしていた。内部名との衝突も拒否する。 |
| 再読み込みで選択欄が初期化される | 初期描画前の空DOMで保存された選択を上書きしていた。初期描画では退避しない。 |
| 不正JSONの読み込み失敗後に内部状態・履歴が変わる | エラー前にstateと履歴を置換していた。失敗時は状態・履歴・下書き・localStorageを戻す。受け入れる保存形式の判定は変更していない。 |
| パネル並べ替えをUndoできない | 履歴登録がなかった。順序が実際に変わった場合だけ1操作として登録する。 |
| 削除したPC固有秘密がRedo・人数変更・再読み込みで復活する | 既定秘密補完が削除済み状態を区別していなかった。承認済みの削除維持仕様に従い削除マーカーを保存する。 |
| 列ドラッグ直後の最初のセルクリックが無視される | ドラッグ後のクリック抑止が次のジェスチャーまで残っていた。新しいpointerdownで解除する。 |
| セル操作後に横スクロールが左端へ戻る | DOM再配置でプレビューのスクロール位置が失われていた。再描画後に復元する。 |

別途、ユーザー確認に従いCanvas全体の自動縮小をやめ、描画サイズを保ってプレビュー内を横スクロールする表示へ変更しました。本人の秘密は公開後も「－」表示を維持し、内部の公開状態とは区別しています。

既存テスト2件はアプリのバグではありませんでした。placeholderをgetByTextで検索する誤りと、閉じたselect内optionへの可視判定を修正しました。

## 検証範囲

- PC人数1〜10の境界、非表示になったPCの知識の保持。
- PC名の入力・反映・未反映入力保持・Undo/Redo・リセット。
- 秘密追加、公開追加、名前変更、衝突拒否、削除、左右移動、列ドラッグ、30件上限。
- ○／×、取得時のみの感情共有、感情方向、プラス／マイナス、片方向／相互、共有の非連鎖、OFF中取得、ON復帰後の新規取得、共有先での手動取消、再取得による再共有。
- 感情の更新・削除後も共有済み情報を保持し、遡及共有しないこと。
- 共有を含む1操作のUndo/Redo、全体公開前の○／×混在状態への復元。
- タイトル反映、サイクル／シーン、人数に応じた選択肢、各選択欄の保持と再読み込み。
- localStorage復元、JSONの実ファイル往復、旧形式（削除マーカーなし）読み込み、不正JSON失敗時の状態保護。
- PNGファイルのシグネチャとCanvas寸法の一致。
- パネル並べ替え・Undo/Redo・保存復元。
- 10人・30秘密、幅1280px／390px、遠い列のドラッグとクリック、横スクロール保持。

Canvasヘルパーは実際のlastLayout、各列の幅、行高、Canvasの実寸と表示寸法から位置を求め、対象が見える位置までスクロールしてから実際のmouse操作を行います。アプリにテスト専用APIは追加していません。

## 保存形式と互換性

任意項目 `suppressedDefaultSecrets`（削除したPC固有秘密の所有PC ID一覧）を追加しました。項目がない既存JSONも読み込めます。削除状態は現在のv8のlocalStorage・JSON・Undo/Redoで保持されます。旧版アプリはこの項目を認識しないため、旧版で開いて再保存したデータについて削除維持は保証しません。正式版の設計は変更していません。

## 残る項目

今回の範囲で再現した問題は修正済みです。未回答の仕様確認はありません。検証環境はWindows上のChromiumです。他ブラウザ・実機タッチ操作は今回の実行範囲に含みません。

## 変更ファイル

- `シノビガミ秘密管理_v8_test.html`：上記修正。
- `tests/shinobigami-v8-debug.spec.js`：既存2件の誤ったlocator/assertionを修正。
- `tests/shinobigami-v8-regression.spec.js`：新規25件。
- `tests/helpers/canvas.js`：横スクロールを考慮した位置計算とドラッグ。
- `playwright.config.js`：v8プロジェクトで追加テストも実行。
- `DEBUG-V8.md`：この報告。

正式版 `シノビガミ秘密管理.html` のSHA256は作業前後で一致:
`3DFCEFE60AB057D4D6C4298966F335DAE4261785CC51827C7EAFFBB820157566`

commit・pushは実行していません。

## 実行結果

| 実行 | 結果 |
| --- | --- |
| 修正前の既存v8 | 21 passed / 2 failed（2件はテスト側の誤り） |
| 最初の新規18件 | 10 passed / 8 failed（不具合を再現） |
| 承認仕様の削除維持・横スクロールを修正前に検証 | 0 passed / 2 failed |
| 横スクロール導入後のドラッグ直後クリック | 修正前に失敗、修正後に成功 |
| 横スクロール位置保持 | 画像確認で発見、追加assertionで失敗を再現、修正後に成功 |
| 最終 npm run test:all | **50 passed / 0 failed（45.0秒）、終了コード0** |
| 新規25件 headed + trace | **25 passed / 0 failed（46.8秒）** |
| 最後のスクロール位置修正後 headed + trace | **1 passed / 0 failed（7.7秒）**、その後に全50件を再実行 |

全実行でアプリのconsole error / page errorは検出されませんでした。NodeのNO_COLOR/FORCE_COLOR警告はブラウザのエラーではありません。

画像を目視し、狭い画面でも文字とマスの大きさを保ち、右側へスクロールした状態で○を操作できることを確認しました。失敗時のスクリーンショットとtrace、成功時のCanvas画像、headedのtraceをレポートに保存しています。

## 正式版への反映（デバッグ完了後）

ユーザーの指示により、この報告のデバッグ完了後にv8候補版を正式版へ反映しました。本文の正式版未変更・commit未実施はデバッグ作業完了時点の記録です。
